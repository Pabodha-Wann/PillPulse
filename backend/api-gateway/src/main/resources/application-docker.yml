spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/pillpulse

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/