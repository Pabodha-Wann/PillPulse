apiVersion: v1
kind: Secret
metadata:
  name: pillpulse-secrets
type: Opaque
stringData:
  KEYCLOAK_CLIENT_SECRET:
  SPRING_MAIL_HOST:
  SPRING_MAIL_PORT:
  SPRING_MAIL_USERNAME:
  SPRING_MAIL_PASSWORD:
  TWILIO_ACCOUNT_SID:
  TWILIO_AUTH_TOKEN:
  TWILIO_PHONE_NUMBER:
  DB_PASSWORD:
